# First complete registered-case inventory, 2026-09-06

[The retained run](../results/all-registered-baseline-20260906/SUMMARY.md)
finished all **22 registered targets**, using the clean locked toolchain,
one-second solver attempts, 600-second per-analyzer wall limits and four jobs.
It returned failure intentionally: **five proof groups and eight calibration
cases were accepted; nine cases were not**. All four selected configured
profiles passed their model checks. No final input-drift failure was reported.

This is a dated baseline, not the verdict on later fixes. In particular it
predates the integrated RISC-V strategy and s390 native receipt routing. It
remains available when a later run changes an outcome.

| Target or group | Recorded outcome | Evidence-backed next action |
| --- | --- | --- |
| Original `string.strnchr` | Unsupported guard diagnostics and pending reviews; ordinary goals are valid. | Review its narrower readable-count domain and exact selected-function model/guard scope. |
| Original `string.strlcat` and naive WP variant | Smoke inconsistency and pending reviews; the intentionally false postcondition also times out. | Correct the reachable trap model; do not copy the stronger variant's unreachable-branch review. The timeout alone does not refute the false claim. |
| Original naive EVA calibration | Six positive existential-precondition rows Unknown; false assertion exported as Invalid or unreachable. Its multiline label was also not recovered. | Establish the positive dependencies and independent reached falsity. The parser's exact multiline mapping has since been corrected; that alone does not accept the case. |
| MPI right shift | All 26 ordinary goals and 14 selected properties valid, but guard diagnostics/model reviews remain. | Check genuine limb types and multibyte alignment/API scope, not a byte-pointer waiver. |
| ARM64 CPUID helpers | All six ordinary goals and ten selected properties valid, but model/guard reviews remain. | Bind exact kernel types, signatures and scalar semantics to the configured build. |
| ARM64 immediates | 102 ordinary goals valid, two termination goals timed out; smoke findings and model reviews remain. | Examine trap/classifier assumptions and unresolved termination dependencies. |
| RISC-V encoders | 81 ordinary goals valid, 38 timeouts; consolidated dependencies also unresolved. | The later complete checked-strategy result must pass common-suite gates before replacing this outcome. |
| Three s390 proof groups | All 159 ordinary goals and 134 selected properties valid; accepted. | Complete the separately registered calibration and named pilot acceptance audit. |
| s390 byte-order calibration | Incomplete without independently authenticated reached falsity. | Integrate the separately retained native witness; report requested z13 versus executed QEMU max explicitly. |
| Eight stronger string calibration cases | All eight accepted with explicit native-6 evidence. | Keep positive dependencies, native input bindings and raw EVA outcomes distinct. |
| Two stronger string proofs | All 106 ordinary goals and 41 selected properties valid; accepted. | Continue clean replay and relocated-workspace acceptance. |

The old trap contract in `annotated/specs.h` requires termination while allowing
neither normal return nor abnormal exit. The original `strlcat` domain permits
its guard branch, so this is not an admissible dead-path assumption for that
variant. The stronger variant has a different documented contract and proves
the branch unreachable. This is a verification-model distinction, not evidence
of a kernel vulnerability; no unsafe execution is needed to diagnose it.

The first generated [coverage inventory](../results/coverage-all-registered-20260906/coverage.md)
retains these 13 dated accepted targets, representing **nine distinct kernel
functions**. Because runner/parser inputs changed after the run, it correctly
reports zero *current* accepted variants. That conservative freshness result does
not erase the dated successes or convert them into kernel defects. Use a later
completed, explicitly supplied run to establish acceptance for later inputs.
