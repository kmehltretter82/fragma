# MIPS32el common24 scoped L2 — 2026-09-07

The registered MT7621 O32/little-endian MIPS32r2 profile now has one accepted
L2 scope: the four explicit-byte 24-bit helpers in
`include/linux/unaligned.h`. The fresh normal run exits zero and reports
`passed`, `accepted=true`, `verified=true`, and both local and proof policy
passing. See the compact [result record](../results/common24-mips32el-reviewed-20260907/SUMMARY.md)
and the preceding [scope review](REVIEW-MIPS32EL-20260907.md).

## Exact result

The run revalidates all 19 configured-profile L1 gates against the genuine
MT7621 SMP/CPS build. It then proves all 94 ordinary WP goals and all 82
selected consolidated properties across the four unchanged kernel bodies and
two project round-trip witnesses. The source inventory is complete, its
dependency closure is unconditional, and it reports no issue or missing link.

All five analyzer warnings match the reviewed plugin, text, severity, function
set, location where present, and bound evidence context. Twelve smoke probes
time out inconclusively; none finds an inconsistency and none is waived as
unreachable. The run also rejects all 22 fixed-input compiler mutations and
both independent type/inline fixture controls.

The positive compiler fixture is ELF32 little-endian `EM_MIPS`, with an
eight-byte function definition and exact O32/MIPS32r2 header flags,
`.reginfo`, `.MIPS.abiflags` and empty `.llvm_addrsig` metadata. The reader is
fail-closed for this fixed input and does not relax generic ELF handling.

## Claim boundary

This is scoped sequential-C L2 for exactly four kernel functions under one
configured profile. It does not establish target execution, MIPS instruction
semantics, linker behavior, kernel callers, traps, MMIO, concurrency,
big-endian MIPS, MIPS64, L3 or whole-architecture verification. The compiler
and proof engines remain explicitly trusted, and the two MIPS-specific review
records retain `implementation_proved: false`.

This proof found no bug. It is verification evidence for functions selected in
advance, not a bug-search result. The separately demonstrated RV32 defect was
found by source review and remains classified that way. The checkpoint's
[full regression](../results/tests-mips-l2-arm32-freeze-20260907/SUMMARY.md)
passes 1,043 tests with 20 conditional skips.
