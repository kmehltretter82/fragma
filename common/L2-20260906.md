# Common24 scoped L2 baselines — 2026-09-06

The four selected Linux byte helpers meet the [PLAN.md L2 criteria](../PLAN.md)
under each of `arm-gcc`, `powerpc32-gcc` and `m68k-gcc`, at pinned Linux
`b9b3e33b70b71e516930117e21de3ad2a7723747`. The fresh registered run passes
all three targets, including current model, source, compiler-calibration,
scoped-review, proof and dependency gates.

This is conditional function-level support for three specific profiles, not
general ARM, PowerPC or m68k verification. The same four kernel functions are
reused; twelve profile/function combinations are not twelve distinct functions.
No L3, target-runtime, generated-code or kernel-caller claim is made.

Scope reviewer: Codex AI assistant. A separate AI assistant independently
checked the completed evidence and publication boundaries. Neither is a human
approval. This document makes the scoped L2 decision after the successful run
and read-only audit; the audit and coverage generator do not themselves award
architecture support levels. No saved result or earlier review was rewritten
to confer acceptance.

## Exact supported scope

All three targets cover `__get_unaligned_be24`, `__get_unaligned_le24`,
`__put_unaligned_be24` and `__put_unaligned_le24`, from
`include/linux/unaligned.h`. The two `fragma_roundtrip_*24` functions are project
witnesses, not kernel functions.

| Registered target | Profile | Model checks passed | Ordinary goals Valid | Selected properties Valid | Retained reviewed warnings |
| --- | --- | ---: | ---: | ---: | ---: |
| `common.unaligned24.arm` | `arm-gcc` | 18 | 94 / 94 | 82 / 82 | 5 |
| `common.unaligned24.powerpc32` | `powerpc32-gcc` | 18 | 94 / 94 | 82 / 82 | 6 |
| `common.unaligned24.m68k` | `m68k-gcc` | 18 | 94 / 94 | 82 / 82 | 5 |

The [unchanged contracts](annotated/unaligned24.verified.c) specify exact decoded
values and bounds, each stored byte, frame conditions and termination. Getters
require only three readable bytes; writers require only three writable bytes
and accept every `u32`. Round trips recover the input modulo 2^24. There is no
extra word alignment, storage extent or `val <= 0xffffff` restriction.

Actual preprocessing preserves all six C bodies, both typedefs, all 24 ordered
ACSL blocks and the 16 intermediate assertions. Source gates check the genuine
kernel bodies and declarators. Every selected access, pointer-formation, shift
and direct-call obligation is retained. Dependency closure is unconditional,
with no unresolved, omitted or trusted proof dependencies; the analysis tools
and explicitly reviewed model assumptions remain trusted.

## Model, review and calibration boundaries

The [scoped review](REVIEW-20260906.md) covers exactly these functions and
profiles. `common24-types` and `common24-frontend` remain reviewed assumptions
with `implementation_proved: false`. Their current bindings include the actual
build, compiler, machine description, preprocessing and analysis policies,
proof strategy, calibration implementation and inspectable review evidence.

Genuine-header fixtures check exact `u8`/`u32` C types, byte alignment, signed
promotion range, signatures and effective inline spelling. ARM32 and m68k
select `no-instrument`; PowerPC32 selects its genuine `patchable-entry-0`
branch. The attribute abstraction covers sequential C behavior only, not
instrumentation, entry padding, linkage behavior, timing or emitted ISA.
The operation-level review explicitly explains the retained Missing RTE,
unsupported-category and disabled-arithmetic warnings; it is not a blanket
warning filter. All applicable selected obligations still have to pass.

Each profile runs a fresh genuine-header fixture, two type/inline mismatch
controls and the [25-command compiler calibration](../docs/common24-compiler-calibration.md).
That is 84 common24 compile/preprocess commands across the three profiles,
including 66 individually wrong value expectations and six type/inline
controls. Exact intended diagnostics and absence of negative objects are
checked. Positive ELF objects retain a defined calibration function and checked
metadata. No object is executed. These are finite compiler-eliminated
observations, not runtime reachability or a substitute for full-domain proofs.

All 12 smoke attempts per profile remain timeout/inconclusive: 36 in total.
`reviewed_smoke` is empty and `consistency_proved` is false. No consistency
theorem or additional ordinary proof is inferred from a smoke timeout.

Kernel callers remain unverified. This scope contains no trap, assembly,
indirect call, MMIO, concurrency or external implementation dependency. It
does not validate those features, compatibility ABIs or other helpers.

## Fresh evidence and independent readback

The [normal run](../results/common24-reviewed-20260906/SUMMARY.md) completed
successfully, exit 0, at **2026-09-06 16:56:16.456553 UTC**, after starting at
16:46:56.436665 UTC. Totals are 54 passing model checks, 282 valid ordinary
goals and 246 Valid selected properties. Counts supplement the exact named,
source-located inventories; counts alone are not the acceptance criterion.

The [read-only audit](../build/common24-reviewed-audit-20260906/REPORT.md)
completed at 16:58:24.413362 UTC, exit 0. It rechecked 1,250 unique file hashes,
including all 1,230 combined integrity inputs, without drift. It reconstructed
the current reviews, compiler/control receipts, raw artifacts, actual frontend
stream and complete proof/dependency inventories. All three coverage readbacks
passed and full production replay was comparable and accepted. The audit reuses
production validators with independent inventory/hash/raw-evidence comparison;
it is not a second implementation of the prover.

This is retained-evidence replay validation, not comparison of two new proof
runs or completed automatic relocation. Compiler readback still requires
unchanged current inputs at their original bound paths. A relevant input
change requires fresh gates and review, not rewriting old hashes.

All [556 evidence-backed project tests](../results/tests-common24-reviewed-final-20260906.log)
pass in 70.892 seconds with no skips. The earlier
[post-review test failure](../results/tests-common24-reviewed-20260906.log) is
preserved: the test expected seven reviewed targets instead of the new ten.
The expected inventory was updated, with explicit checks for the three new
profile-bound reviews; production acceptance checks were not weakened.

The [explicit coverage report](../results/coverage-common24-reviewed-20260906/coverage.md)
retains all 25 targets, 21 architectures and 24 distinct kernel functions.
Exactly three targets are accepted-current, covering four distinct functions.
Sixteen older acceptances are stale and six legacy nonpasses remain visible;
18 distinct functions have a latest reported accepted variant at its dated
identity. Independent readback checked all 3,041 generation-input hashes and
570 retained artifact references (546 unique paths), without drift.

The generated architecture-level column deliberately remains `not-assessed`;
current dated model evidence for the three new profiles is L1. This document
records the separate, narrowly scoped L2 conclusion. Together with the
[dated s390 pilot](../s390/PILOT-20260906.md), four architecture families now
have named L2 baselines, of which these three are freshly accepted here.
Seventeen architectures still lack a documented L2 baseline; older s390 and
other suite results also need renewed current-input acceptance.

Exact SHA-256 identities:

```text
4a5144a469d6607ede10ef303e53df8e4dcb495d0713f86a2ed5f2a149ed513b  results/common24-reviewed-20260906/summary.json
3d2acf486408318160ca6b4db9698221234c478099fa488a263484289e2f13d2  build/common24-reviewed-audit-20260906/audit-1.json
363feb5b50107bb4afad2ada5614a4795b0cce062d858bd27fe314143a12606b  common/REVIEW-20260906.md
ec46702734d340290a3801836b0199aceddaf7aa6042d07a35e47d340269863c  config/common-byte-targets.json
703ffc6da446b267c3f2c7eaeba2ff6def5ec2e9b2637b58ab070ab3682677b2  results/tests-common24-reviewed-final-20260906.log
ed909754da5327a06312eb862bbf5d87a675c9859f70332ae0ee870b921e03ec  results/coverage-common24-reviewed-20260906/coverage.json
```

These are locally checked file identities, not tamper-resistant attestations.
The [first unaccepted integrated run](INTEGRATION-20260906.md) and intermediate
compiler-only diagnostics remain dated evidence with their original statuses.

## Continue the architecture work

Use the [common harness instructions](README.md) for a fresh run with a new
output directory; no installation is needed on this machine. The
[next-profile queue](NEXT-PROFILES.md) starts with ARM64, RISC-V and SH.
Alpha needs a narrowly justified ELF metadata allowance; hardware x86-64 and
UML need explicit support for their genuine `-Os` build policy. These are
future work, not inherited acceptance from the three profiles above. Remaining
L1 ports, all-architecture L2, broader CI/reproducibility and caller coverage
remain open under PLAN.md.
