# Renewed s390x seven-helper L2 scope, 2026-09-07

The existing seven-helper pilot again meets the [PLAN.md L2 criteria](../PLAN.md#meaning-of-architecture-support)
under the current `s390x-gcc` profile and pinned Linux revision
`b9b3e33b70b71e516930117e21de3ad2a7723747`. This is a new dated scope decision
for the same functions and contracts as [the original pilot](PILOT-20260906.md),
not a new architecture, whole-profile support, kernel-caller verification or
whole-pilot L3. Scope reviewer: the primary project assistant, after a separate
assistant's independent current-evidence audit.

## Exact renewed scope

| Mandatory target | Kernel helpers | Ordinary goals Valid | Selected positive properties Valid |
| --- | --- | ---: | ---: |
| `s390.unaligned24` | Four BE/LE 24-bit getters and setters | 94 | 82 |
| `s390.unaligned48` | BE48 getter and setter | 86 | 71 |
| `s390.tod_to_ns` | `tod_to_ns` | 3 | 5 |
| `calibration.s390.byte-order.eva` | Reuses BE24; no additional helper | — | 16 |

The [three WP groups](../results/noncommon-clang-renewed-s390-20260907/summary.json)
completed at 01:47:33.807686 UTC. The
[mandatory byte-order calibration](../results/calibrations-clang-renewed-s390-20260907/summary.json)
completed at 02:19:52.077050 UTC. Both authentic summaries are terminal and
accepted. They are not merged into an invented run. The earlier single-summary
pilot auditor was not rerun against a synthetic input; its exact inventories
and predicates were checked independently against these two retained summaries.

The WP scope retains 183 ordinary goals, 158 selected Valid properties and all
23 named postconditions. The three round-trip functions remain project
witnesses, not kernel functions. Exact historical goal/property identity hashes
and pointer predicates match, not only their counts. Each byte group closes
12 ordinary and 12 exported pointer guards; the RTE-before-Eva case closes its
three additional pointer guards under the explicit `object-or-null` policy.

## Source, model and assumption boundary

The existing source/fixture/strategy hashes and target manifest remain
byte-identical. Decoders retain their readable three-/six-byte preconditions;
encoders retain writable regions of the same sizes and the entire `u32`/`u64`
input domains. Weighted decode, exact output bytes and round-trip modulo
2^24/2^48 claims are unchanged. TOD retains the whole 64-bit unsigned-long
domain and its exact conversion contract. No new axiom, hidden input bound,
trap stub or external function contract was introduced to close a proof.

Both current s390 models pass all 18 configured L1 checks, including genuine
kernel compiler/header checks, exact width/endian negative controls and the
one-field native-memory-endian discriminator. The recorded build retains
`-march=z13 -mtune=z13`. Source/review/header gates and actual analysis-policy
audits are current. All 15 WP warnings retain individually scoped reviews;
the 12+6 smoke attempts remain inconclusive, not consistency proofs.

The independent four-target audit rechecks raw WP/TSV results, current
assumptions/reviews, dependency identities and re-evaluated acceptance. Its
1,253 unique recorded inputs are unchanged; all 880 retained files are unchanged
across before/after readback. The audit is execution-tool output, not a new
saved pilot-audit JSON or a fresh run of the old 17-test auditor suite.

## Calibration and runtime boundary

All 16 positive calibration properties remain Valid. The deliberately false
byte-order assertion retains its raw `Invalid or unreachable` label. The
[native-5 receipt](../build/s390-sensitivity/native-5/receipt.json) was independently
revalidated against current model/build/source/artifact identities and reached
observations; it was not regenerated or executed again. Its one bounded
valid-input observation corroborates the false specification, not a kernel bug.

The recorded target is z13, but the actual historical emulator CPU is QEMU
`max`; exact-z13 startup failed before guest execution and remains preserved.
The expoline return backend remains trusted native support, not an assembly
theorem. This observation does not establish full z13 fidelity, all helper
inputs, stores, 48-bit or TOD runtime behavior. No whole-pilot L3 follows.
Kernel callers, broader trap behavior, ABI variants, assembly, concurrency and
MMIO remain outside the renewed scope.

The [current matrix](../results/coverage-calibrations-clang-renewed-20260907/coverage.md)
preserves all six unrelated legacy nonpasses. Its architecture-level cells
still award no L2/L3; this document supplies the separate scoped L2 decision.
Together with the nine current common24 baselines, ten families now have current
named L2 scopes. Eleven other architecture baselines remain unfinished.

```text
60e58ae2ce83fdd4ee79aeee93bfdb0d809f6e58a9e99204abfbd722f177ff41  WP summary.json
7afccfc966880748c529c0e9489b700eeff4c69beebdb6100f9bb6acb50d9e4c  Eva summary.json
70e6f6832f189533d412692ec1b9a0f58436910ad18fdc11e67e695bc9e0b6df  native-5/receipt.json
```
